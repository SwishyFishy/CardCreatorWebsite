export type TypeData = {
    types: string[],
    readonly id?: "type"
}

export default function CardType({types}: TypeData)
{
    return(
        <div className="component-cardtype">

        </div>
    );
}